# 서울 대장아파트 퀴즈 - 개발 가이드

## 프로젝트 구조

- **`index.html`** — 게임 전체 (HTML/CSS/JS 단일 파일)
- **`README.md`** — 수록 단지 목록
- **`.github/workflows/deploy.yml`** — push 시 자동 배포

## 배포 흐름

`main` 브랜치 push → GitHub Actions → `jyy1554/at-game_seoul` gh-pages → **playatgame.com/seoul-apt/**

---

## 새 단지 추가 방법

### 1. 단지 정보 수집 (호갱노노 API)

hogangnono.com에서 아래 API로 세대수·입주연도·정확한 단지명을 가져온다.

```
GET https://hogangnono.com/api/v2/searches/suggestions/new?query={단지명}&x=126.9784&y=37.5665
```

응답에서 `data.matched.apt.list[0]` 의 필드를 사용:
- `name` → 호갱노노 기준 단지명 (이게 게임에 표시되는 이름)
- `household` → 세대수
- `start_date` → 입주년월 (연도만 추출)
- `address` → 법정동 확인용

### 2. 이미지 URL 확보

KB부동산(kbland.kr) 또는 다른 부동산 사이트에서 단지 대표 사진 URL을 직접 찾아온다.  
이미지가 없으면 `img:""` 로 비워두면 퀴즈 출제에서 자동 제외됨.

### 3. index.html APTS 배열에 추가

`index.html` 내 `const APTS = [...]` 배열에 아래 형식으로 항목 추가:

```js
{name:"법정동 단지명", gu:"○○구", units:세대수, year:입주연도, highlight:"한줄특징", img:"이미지URL"},
```

**name 형식**: `"{법정동} {호갱노노 단지명}"` — 예: `"압구정동 현대14차"`, `"잠원동 아크로리버뷰신반포"`

**highlight 작성 기준**: 게임 화면에 chip으로 표시되는 한 줄 소개. 10자 내외.  
아래 패턴 중 단지의 가장 핵심적인 특징 하나를 고른다.

| 패턴 | 예시 |
|---|---|
| 위치 + 시세 특징 | `"신반포 한강뷰 최고가"`, `"압구정 재건축 핵심 단지"` |
| 위치 + 규모 | `"개포 최대 규모"`, `"전국 최대 단지"` |
| 뉴타운/재건축 | `"흑석뉴타운 대장"`, `"아현뉴타운 대단지"` |
| 랜드마크 | `"한국 최고가 아파트 대명사"`, `"청량리 랜드마크 신축"` |
| 학군 | `"대치 학군 대장"`, `"중계동 학군 단지"` |
| 역세권 | `"공덕역세권 자이"`, `"서울대입구역 대단지"` |
| 잘 알려진 별칭·수식어 | `"잠실 4대 단지"`, `"반포 한강변 대표"` |

단지 성격을 잘 모르면 hogangnono 태그(검색 결과의 `tag` 필드)나 네이버 부동산 단지 소개를 참고.

### 4. README.md 수록 단지 목록 업데이트

`README.md`의 수록 단지 표에서 해당 구 행에 단지명 추가.

### 5. 배포

```bash
git add index.html README.md
git commit -m "Add 단지명"
git push
```

push 하면 GitHub Actions가 자동으로 playatgame.com/seoul-apt/ 에 배포.

---

## 주의사항

- **단지명은 무조건 호갱노노 기준** (seoulowner 등 다른 사이트 이름과 다를 수 있음)
- **이미지 출처 언급 금지** — 저작권 이슈로 이미지 소스(KB부동산 등)를 게임 내·README에 명시하지 않음
- **배포 전 로컬 미리보기 필수** — 구린 상태로 배포하지 말 것
- **`img:""`인 단지는 퀴즈에서 자동 제외됨** — 이미지 없이 추가해도 게임에 영향 없음

---

## 현재 수록 현황

- **총 99개** 단지 (25개 구 전체 수록)
- **이미지 있는 단지**: 99개 (전 단지 퀴즈 출제 대상)

seoulowner.co.kr 구별 1~3위 기준으로 선정.

**중복 제외 원칙**: 단지명에서 숫자(차수·단지 번호)만 다르고 나머지가 같은 경우 가장 대표적인 것 1개만 수록.  
예) 금천롯데캐슬골드파크 **1차** ← O, 2차·3차 ← X  
예) 목동신시가지 **1단지** ← O, 3단지·7단지·14단지 ← X  
예) 광장극동 **1차** ← O, 2차 ← X
